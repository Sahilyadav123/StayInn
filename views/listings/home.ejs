<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Your Stay</title>

    <!-- External CSS for smooth animation -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: url('https://images.unsplash.com/photo-1561501900-3701fa6a0864?w=600&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8Mnx8bHV4dXJ5JTIwaG90ZWx8ZW58MHx8MHx8fDA%3D') no-repeat center center fixed;
            background-size: cover;
            overflow: hidden;
        }

        /* Welcome screen container */
        .welcome-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: white;
            text-align: center;
            position: relative;
            overflow: hidden;
            animation: fadeInBackground 5s ease-in-out;
        }

        /* Fade-in for background */
        @keyframes fadeInBackground {
            0% {
                filter: brightness(50%);
                transform: scale(1.2);
            }
            100% {
                filter: brightness(100%);
                transform: scale(1);
            }
        }

        /* Text animation from left and right */
        h1, p {
            opacity: 0;
            font-weight: bold;
            transform: translateX(-50px);
            animation: textAppear 2s forwards;
        }

        h1 {
            animation-delay: 0.5s;
        }

        p {
            animation-delay: 1.5s;
        }

        @keyframes textAppear {
            0% {
                opacity: 0;
                transform: translateX(-50px);
            }
            50% {
                opacity: 1;
                transform: translateX(0);
            }
            100% {
                opacity: 1;
                transform: translateX(0);
            }
        }

        /* Highlighted text */
        h1 {
            font-size: 3rem;
            color: #ffeb3b;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.7);
        }

        p {
            font-size: 1.5rem;
            color: #ffffff;
            text-shadow: 1px 1px 3px rgba(0,0,0,0.7);
        }

        /* Styling for the button */
        .welcome-button {
            margin-top: 30px;
            padding: 15px 40px;
            background-color: #28a745;
            color: white;
            border: none;
            border-radius: 50px;
            font-size: 1.5rem;
            cursor: pointer;
            transition: transform 0.3s ease, background-color 0.3s ease;
            animation: bounceIn 2s forwards;
            animation-delay: 3s;
        }

        @keyframes bounceIn {
            0% {
                opacity: 0;
                transform: translateY(100px);
            }
            50% {
                opacity: 1;
                transform: translateY(-10px);
            }
            100% {
                transform: translateY(0);
            }
        }

        .welcome-button:hover {
            background-color: #218838;
            transform: scale(1.05);
        }

        /* Floating animation for elements */
        .floating {
            position: relative;
            animation: floating 6s ease-in-out infinite;
        }

        @keyframes floating {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-20px);
            }
        }

        /* Star-like background animation */
        .background-animation {
            position: absolute;
            width: 100%;
            height: 100%;
            z-index: -1;
            top: 0;
            left: 0;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 1%, rgba(255,255,255,0) 50%);
            background-size: 10px 10px;
            animation: twinkling 10s infinite;
        }

        @keyframes twinkling {
            0% { transform: translateY(0); }
            100% { transform: translateY(-100px); }
        }
    </style>
</head>

<body>

    <!-- Background Animation -->
    <div class="background-animation"></div>

    <!-- Welcome Screen -->
    <div class="welcome-container">
        <div class="floating">
            <h1>Welcome to Your Dream Stay</h1>
            <p>Experience the comfort and luxury at our premium hotels</p>
            <!-- Let's Go Button -->
            <form action="/listings" method="GET">
                <button class="welcome-button">Let's Go</button>
            </form>
        </div>
    </div>

</body>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>

</html>
